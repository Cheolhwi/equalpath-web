# Revision 2.1 acceptance ledger

46 stories / 114 AC. First round: Epics 1–3, 18 stories / 43 AC. No criterion is silently renumbered.

Implemented means the behavior exists and listed evidence was exercised; it does not certify every real-data prerequisite or shared quality gate. Formal release remains open as described in PHASE0_3_VERIFICATION.md.


## Epic 1

| AC | Exact authoritative criterion | State / evidence |
| --- | --- | --- |
| 1.1.1 | Given a visitor has no saved information, when she submits a public pickup place, service date, collection deadline and care end time, then a candidate list or explained empty result appears without account or child-profile setup. | **Implemented / functional checks**. 真实 Appwrite 搜索、必填输入错误与可选年龄/接送；浏览器和 domain 测试。 |
| 1.1.2 | Given an essential field is missing or care end is not later than collection deadline, when she submits the request, then the affected fields are named, valid entries remain and no new assessment is shown. | **Implemented / functional checks**. 真实 Appwrite 搜索、必填输入错误与可选年龄/接送；浏览器和 domain 测试。 |
| 1.1.3 | Given age or transport preference has not been selected, when she starts the search, then results identify those conditions as unspecified and allow her to refine them without restarting. | **Implemented / functional checks**. 真实 Appwrite 搜索、必填输入错误与可选年龄/接送；浏览器和 domain 测试。 |
| 1.2.1 | Given she needs pickup and care, when she chooses institutional pickup, then results distinguish published institutional transport, explicitly unavailable transport and unknown transport; unknown coverage is visible. | **Implemented / real-data gate open**. 机构接送 / 自送分离、未知覆盖；真实自送路径和受控三状态测试。 |
| 1.2.2 | Given she can arrange delivery, when she selects self-arranged delivery, then institutional transport is no longer a required condition and her other request details remain. | **Implemented / functional checks**. 机构接送 / 自送分离、未知覆盖；真实自送路径和受控三状态测试。 |
| 1.3.1 | Given the age choices defined in this specification, when she selects age 4, then the request uses the interval from the fourth birthday up to the fifth birthday without requesting a date of birth. | **Implemented / functional checks**. 完成年龄区间、全包含/排除/部分重叠；真实 EDWETHINK 年龄来源与 domain 测试。 |
| 1.3.2 | Given a sourced admission interval fully includes or excludes the selected age interval, when she checks age relevance, then it shows supported by published ages or an explicit age conflict, with the published wording and source. | **Implemented / functional checks**. 完成年龄区间、全包含/排除/部分重叠；真实 EDWETHINK 年龄来源与 domain 测试。 |
| 1.3.3 | Given a selected interval only partly overlaps the published range or its endpoints are ambiguous, when she checks age relevance, then it shows age needs confirmation and explains the uncertainty; missing ages are also unknown. | **Implemented / functional checks**. 完成年龄区间、全包含/排除/部分重叠；真实 EDWETHINK 年龄来源与 domain 测试。 |
| 1.4.1 | Given source-backed locations exist, when she opens a candidate location, then she can identify both the pickup place and institution and inspect its care, hours and transport facts; any displayed distance states its basis. | **Implemented / functional checks**. 实际地图/列表选中一致；位置缺失保留；隔离地图故障下仍能打开详情。 |
| 1.4.2 | Given a candidate location is missing or the map is unavailable, when she browses results, then the list and sourced area labels remain accessible with a location limitation; no invented coordinate or journey time appears. | **Implemented / functional checks**. 实际地图/列表选中一致；位置缺失保留；隔离地图故障下仍能打开详情。 |
| 1.5.1 | Given she has an active search, when she applies changed request details, then candidates and current fit results use the revised actual request and unchanged fields remain. | **Implemented / functional checks**. 修改后的期限和结束时间重新检查；无效输入/503 保留旧结果标识；不覆盖等待期间的新输入。 |
| 1.5.2 | Given the new search is pending or fails, when she inspects the page, then any retained result is labelled with its earlier request and a retry is offered; it is not presented as a result of the revision. | **Implemented / functional checks**. 修改后的期限和结束时间重新检查；无效输入/503 保留旧结果标识；不覆盖等待期间的新输入。 |
| 1.6.1 | Given no candidates meet the selected filters, when she opens the empty result, then active restrictions are shown and she can deliberately change an area or include unknown service information without changing other inputs. | **Implemented / functional checks**. 不存在关键词显示限制及修改入口；503 和重试恢复实测。 |
| 1.6.2 | Given the search source or service is unavailable, when she submits a request, then an availability message and retry appear instead of a claim that no local services exist. | **Implemented / functional checks**. 不存在关键词显示限制及修改入口；503 和重试恢复实测。 |

## Epic 2

| AC | Exact authoritative criterion | State / evidence |
| --- | --- | --- |
| 2.1.1 | Given a source explicitly describes one-off admission or an age restriction, when she inspects the candidate, then the wording, source and applicable request condition appear together. | **Implemented / functional checks**. 真实 Little Whale hourly-care 目录证据、普通入托保持未知；受控规则测试。 |
| 2.1.2 | Given only daycare or a monthly programme is described, when she inspects admission, then one-off admission remains unknown with the missing policy identified; ordinary enrolment is not treated as temporary acceptance. | **Implemented / functional checks**. 真实 Little Whale hourly-care 目录证据、普通入托保持未知；受控规则测试。 |
| 2.2.1 | Given a source names self-operated or partner transport and its service area, when she opens transport details, then transport type and coverage appear separately with their source. | **Implemented / real-data gate open**. 自营/合作类型与覆盖独立；只有运输服务不补覆盖；受控测试。 |
| 2.2.2 | Given a source states only that transportation exists, when she checks her pickup place, then transport existence is visible while coverage and same-day capacity remain unknown. | **Implemented / real-data gate open**. 自营/合作类型与覆盖独立；只有运输服务不补覆盖；受控测试。 |
| 2.3.1 | Given pickup times applicable to the requested day are published, when she checks the deadline, then a window wholly after the deadline is a conflict, a window wholly at or before it meets the published timing condition, and a window spanning it needs confirmation. | **Implemented / real-data gate open**. 三种接走窗口状态有测试；浏览器修订期限后状态变化；转送/到达未知。 |
| 2.3.2 | Given pickup times or their applicability are missing, when she checks the deadline, then pickup timing needs confirmation; transfer duration and arrival remain unknown because this baseline supplies no route-time calculation. | **Implemented / functional checks**. 三种接走窗口状态有测试；浏览器修订期限后状态变化；转送/到达未知。 |
| 2.4.1 | Given published opening hours apply to the requested day, when she checks final collection, then those hours are used as the care window and her end time is compared with them; a more specific care schedule and sourced late-collection restriction take precedence. | **Implemented / functional checks**. 2026-09-13 用户批准 Revision 2.1：营业时间参与照护结束检查；电话仅展示并保留来源；不要求系统拨号。边界和例外规则已测试。 |
| 2.4.2 | Given care or opening hours are missing or date-specific holiday hours remain unresolved, when she checks final collection, then timing remains unknown with a reason; a sourced closed day is a conflict and a timing match does not confirm one-off admission or capacity. | **Implemented / functional checks**. 2026-09-13 用户批准 Revision 2.1：营业时间参与照护结束检查；电话仅展示并保留来源；不要求系统拨号。边界和例外规则已测试。 |
| 2.5.1 | Given a candidate has been checked, when she opens its fit explanation, then age, admission, transport existence, pickup coverage, pickup timing and care end each show supported, conflict or unknown with a reason. | **Implemented / functional checks**. 详情与比较每项显示文字状态、原因和来源；真实和受控页面验收。 |
| 2.5.2 | Given at least one decisive condition is unknown, when she reads the summary, then that condition remains visible and the page does not label the whole arrangement accepted or confirmed. | **Implemented / functional checks**. 详情与比较每项显示文字状态、原因和来源；真实和受控页面验收。 |

## Epic 3

| AC | Exact authoritative criterion | State / evidence |
| --- | --- | --- |
| 3.1.1 | Given a supported branch-to-record match exists, when she opens registration evidence, then authority, source record, published name, category, identifiers and available validity dates appear with the original-source link. | **Implemented / functional checks**. JKM 名称/号码/类型/日期/原来源；另源地址与电话；分支不符隔离。 |
| 3.1.2 | Given the reused JKM import has no address or telephone, when she opens its evidence, then those fields are labelled not included in this import; any separately acquired address or contact has its own source and is not attributed to that import. | **Implemented / functional checks**. JKM 名称/号码/类型/日期/原来源；另源地址与电话；分支不符隔离。 |
| 3.1.3 | Given a name is shared by several branches and supporting identity evidence is insufficient, when she opens registration evidence, then the match stays unresolved and no other branch record is presented as verified for this one. | **Implemented / functional checks**. JKM 名称/号码/类型/日期/原来源；另源地址与电话；分支不符隔离。 |
| 3.2.1 | Given a recorded validity term has ended or identity facts conflict, when she inspects status, then the precise issue and source date are shown; renewal is unknown unless separately evidenced. | **Implemented / functional checks**. 目录 KPM 代码不作为官方核验；到期与续期未知；受控异常状态。 |
| 3.2.2 | Given only a directory claim or another branch record is available, when she inspects status, then the candidate is not labelled officially verified and she can inspect the source of the claim. | **Implemented / functional checks**. 目录 KPM 代码不作为官方核验；到期与续期未知；受控异常状态。 |
| 3.3.1 | Given she selects at least two candidates, when she opens comparison, then the same factors and uncertainty labels are shown for each candidate. | **Implemented / functional checks**. 最多三机构同需求比较；分支事实独立，缺项留在原机构。 |
| 3.3.2 | Given one candidate lacks a fact or has conflicting evidence, when she compares it, then that gap remains in its own entry and is not filled with information from another branch. | **Implemented / functional checks**. 最多三机构同需求比较；分支事实独立，缺项留在原机构。 |
| 3.4.1 | Given comparable facts exist, when she selects one supported priority, then the candidate order and factor used are visible and repeatable. | **Implemented / functional checks**. 距离、营业关门、运输、名字排序；未知值后置、无事实选项禁用；浏览器排序测试。 |
| 3.4.2 | Given some candidates lack the chosen factor, when she orders them, then unknown values are labelled and placed after candidates with comparable values; they are not treated as zero or best fit. | **Implemented / functional checks**. 距离、营业关门、运输、名字排序；未知值后置、无事实选项禁用；浏览器排序测试。 |
| 3.4.3 | Given the requested ordering requires absent facts, when she opens priorities, then that option is marked unavailable with a reason and she can choose another; distance ordering requires a stated distance basis. | **Implemented / functional checks**. 距离、营业关门、运输、名字排序；未知值后置、无事实选项禁用；浏览器排序测试。 |
| 3.5.1 | Given a sourced fee is available, when she opens costs, then its amount, currency, charging unit and applicable conditions appear together with the source. | **Implemented / functional checks**. 公开官网采集并按分店匹配，新增 Rising Stars 和两家 Children’s Discovery House 的 15 项费用；原单位、适用条件、来源与获取日期保留。完整临时总价仍需规则。 |
| 3.5.2 | Given only monthly fees or incomplete charges are known, when she compares costs, then the published basis and missing charge categories appear, and the total is labelled unavailable; no hourly conversion is invented. | **Implemented / functional checks**. 显示原币种/单位/条件与缺项；月费不换算；受控完整费率能展开估算。 |
| 3.5.3 | Given the team has validated an applicable one-off rate and all required calculation rules, when she requests a reference total, then its duration, minimum charge, included extras and calculation are shown as an estimate requiring provider confirmation. | **Implemented / real-data gate open**. 显示原币种/单位/条件与缺项；月费不换算；受控完整费率能展开估算。 |
| 3.6.1 | Given the request and candidate contain unanswered conditions, when she prepares the list, then admission, pickup coverage, timing and charges are ordered by relevance to her request. | **Implemented / functional checks**. 生成、去重、调整、取消和复制问题；需求/机构上下文保留；不读评论表。 |
| 3.6.2 | Given the same unknown appears more than once, when she prepares the list, then it appears as one question retaining the candidate and request context. | **Implemented / functional checks**. 生成、去重、调整、取消和复制问题；需求/机构上下文保留；不读评论表。 |
| 3.6.3 | Given the review feature is unavailable or she selects no review concern, when she prepares or copies the list, then a usable service-question list is still produced without requiring review data. | **Implemented / functional checks**. 生成、去重、调整、取消和复制问题；需求/机构上下文保留；不读评论表。 |
| 3.7.1 | Given a branch-matched public institutional phone number has a source, when she opens contact options, then the readable number and source are displayed with her prepared questions; no device telephone action or call completion is required. | **Implemented / functional checks**. 2026-09-13 用户批准 Revision 2.1：营业时间参与照护结束检查；电话仅展示并保留来源；不要求系统拨号。边界和例外规则已测试。 |
| 3.7.2 | Given a phone number is unavailable, when she opens contact options, then phone unavailable and the institution source page are shown with copyable questions; no number or contact outcome is invented. | **Implemented / functional checks**. 真实机构电话来源及 tel URL 已检查；来源链接、无电话恢复；页面间返回保留清单。 |
| 3.7.3 | Given she returns from the contact view or source page in the same browser task, when she resumes comparison, then the selected candidates and questions remain and no contacted, accepted or booked status is inferred. | **Implemented / functional checks**. 2026-09-13 用户批准 Revision 2.1：营业时间参与照护结束检查；电话仅展示并保留来源；不要求系统拨号。边界和例外规则已测试。 |

## Epic 4

| AC | Exact authoritative criterion | State / evidence |
| --- | --- | --- |
| 4.1.1 | Given she chooses a candidate, when she saves it and later opens saved institutions, then its branch reference and optional non-identifying reason are available for reopening. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 4.1.2 | Given she reopens a favourite, when she starts a new request for it, then she chooses the service date and current conditions are checked rather than restoring an old fit decision. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 4.1.3 | Given browser storage is unavailable or full, when she saves an institution, then a not-saved message appears and the current candidate remains available; the save view explains that saved items stay in this browser. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 4.2.1 | Given she saves request details, when she starts another request from that template, then the pickup place, care end preference and transport need are prefilled and she selects a fresh service date. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 4.2.2 | Given the saved place cannot be matched or saved times conflict with the new request, when she uses the template, then the affected field needs correction and is not silently replaced. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 4.2.3 | Given she opens the save explanation, when she reviews what is retained, then she sees the allowed fields, current-browser storage and possible loss after browser clearing; no automatic cross-device recovery is promised. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 4.3.1 | Given saved evidence versions differ from current published versions, when she reopens a saved option, then material differences and their dates appear and she can check them against the new request. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 4.3.2 | Given a current refresh has failed or there is no comparable earlier fact, when she reviews the option, then the page states what cannot be compared and retains the last known dates without claiming that nothing changed. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 4.4.1 | Given she chooses a saved item, when she applies an edit or deletion, then her saved list and subsequent reuse reflect that choice while provider source facts remain unchanged. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 4.4.2 | Given the local change cannot be saved, when she applies it, then failure is shown and the previous saved version remains identified rather than claiming the change succeeded. | **Deferred — later phase**. Not implemented in phases 0–3. |

## Epic 5

| AC | Exact authoritative criterion | State / evidence |
| --- | --- | --- |
| 5.1.1 | Given a request and candidate are selected, when she creates preparation material, then the prompts reflect transport mode, care duration and known service requirements, separating general prompts from provider-sourced requirements. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 5.1.2 | Given she uses the same provider with changed times or transport, when she prepares the new occasion, then the prompts reflect that occasion and no previous successful visit is required. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 5.2.1 | Given handover questions exist, when she opens preparation, then each question is grouped under the party able to answer it; provider-selection questions remain in the contact enquiry list. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 5.2.2 | Given the transporter or receiving contact is unspecified, when she checks the questions, then the relevant party is labelled to be confirmed rather than being assigned by the website. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 5.3.1 | Given known locations and times exist, when she opens the sequence, then each step labels its information as a request assumption, published fact or unresolved item. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 5.3.2 | Given transfer duration or arrival time is unsupported, when she opens the sequence, then that step shows arrival to be confirmed and no journey time is calculated from coordinates. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 5.4.1 | Given she opens handover guidance, when she reviews release arrangements, then prompts cover the release procedure at the usual centre, collector identification, receiving party and delay contact. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 5.4.2 | Given another person reads the guidance, when the material is opened or printed, then its draft status and the need for institution-managed authorisation are explicit. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 5.5.1 | Given preparation prompts are available, when she opens the checklist, then care-appropriate items and provider-specific published requirements are distinguishable. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 5.5.2 | Given the receiving institution needs identity or health information, when she reviews the information checklist, then it directs her to provide it privately and offers blank print-only spaces without online entry fields. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 5.5.3 | Given she changes the care duration or transport arrangement, when she regenerates the checklist, then relevant prompts update without asking her to maintain a child profile. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 5.6.1 | Given a preparation draft exists, when she prints or downloads it, then the sequence, sourced institutional contacts when available, unanswered questions and preparation date are readable without creating a shared card. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 5.6.2 | Given the draft contains offline information prompts, when she exports it, then those spaces remain blank and the document states that authorisation and provider agreement must be arranged separately. | **Deferred — later phase**. Not implemented in phases 0–3. |

## Epic 6

| AC | Exact authoritative criterion | State / evidence |
| --- | --- | --- |
| 6.1.1 | Given a permitted branch-matched review exists, when she opens an excerpt, then the passage, route to original context, publication date or date uncertainty, and retrieval time are distinguishable. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 6.1.2 | Given only positive or only negative experiences support a topic, when she reads the excerpts, then that evidence is shown without an invented opposing quote; allegations remain attributed opinions. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 6.1.3 | Given reviewer identity or publication date cannot be verified, when she inspects the review, then the limitation is visible and retrieval time is not substituted for the publication date. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 6.2.1 | Given supported topic labels exist, when she selects a topic, then relevant passages appear with access to their context. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 6.2.2 | Given the collected sample contains no relevant passage, when she selects the topic, then no topic evidence is shown explicitly instead of unrelated praise. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 6.3.1 | Given dated reviews exist, when she opens the evidence summary, then collected counts, date distribution and the recent twelve-month subset appear. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 6.3.2 | Given collection is partial or dates are unknown, when she checks the summary, then collection scope and undated counts are visible and no complete-platform coverage claim is made. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 6.3.3 | Given a topic has fewer than two dated relevant reviews in the recent twelve months, when she checks that topic, then a limited recent evidence label and the count appear; this is a display threshold, not a reliability score. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 6.4.1 | Given reviews and other page material have been collected, when she opens experiences, then care-related reviews are shown separately and promotional or editorial descriptions are excluded from review counts. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 6.4.2 | Given an item is excluded by a relevance rule or is old, when she opens the explanation, then its classification or date reason is available without describing all excluded or older material as false. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 6.5.1 | Given a concern appears in at least two distinct collected reviews, when she inspects the recurring-concern suggestion, then its supporting excerpts and sample limitation are visible. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 6.5.2 | Given she selects a concern, when she adds it to her enquiry list, then the contact page shows a neutral question with evidence and merges any duplicate through the same list used for service questions. | **Deferred — later phase**. Not implemented in phases 0–3. |

## Epic 7

| AC | Exact authoritative criterion | State / evidence |
| --- | --- | --- |
| 7.1.1 | Given candidates or favourites are available, when she creates a card, then selected branch references appear in her chosen order with optional approved reasons. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 7.1.2 | Given she can edit the card, when she changes its membership or order, then the card revision reflects the change while published provider facts remain unchanged. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 7.1.3 | Given a local favourite is also referenced by a card, when she deletes that favourite and opens the card, then the card retains its separately chosen contents and editing the card remains a separate action. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 7.2.1 | Given an active read-only card exists, when the recipient opens it on a phone without signing in, then the first contact, next backup and critical timing limits are visible, with institutional locations, contacts and further details available. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 7.2.2 | Given a phone, time or transport fact is unknown or conflicting, when the recipient reads the card, then the limitation and remaining questions are explicit and no confirmed reception is implied. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 7.2.3 | Given the card has been revoked or cannot be retrieved, when the recipient opens its link, then the relevant access or availability state is shown without displaying a cached private card. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 7.3.1 | Given she creates a shared card, when she chooses to share for reading, then separate unpredictable read and edit capabilities are supplied and the read view reveals no edit credential. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 7.3.2 | Given a person has only the read capability, when that person attempts to edit or revoke the card, then the change is refused and the card remains unchanged. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 7.3.3 | Given a person has an edit capability for a different card, when that person attempts to edit or revoke this card, then access is refused and neither card is changed. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 7.3.4 | Given the editor opens the management explanation, when she reviews link handling, then it explains that anyone holding the edit link can manage this card and a lost edit link cannot be recovered through the read link. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 7.3.5 | Given only a public card identifier or an invalid capability is supplied, when private card content is requested, then access is denied without disclosing the card. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 7.4.1 | Given a preparation draft exists, when she chooses content to attach, then the card includes the approved checklist and sequence with their preparation date. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 7.4.2 | Given local notes or print-only identity prompts exist, when she attaches preparation, then private notes and completed offline information are excluded and blank prompts grant no pickup authority. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 7.5.1 | Given an active card exists, when she exports it, then the ordered contacts, critical unknowns, card revision date and read-only online checking reference are legible. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 7.5.2 | Given preparation has not been attached, when she exports the card, then the contact plan is still usable without requiring Epic 5 material. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 7.5.3 | Given the exported document is inspected, when the reader looks for management access or current status, then no edit credential is present and the copy explains that later online changes or revocation cannot update it. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 7.6.1 | Given underlying facts have changed, when a reader reopens the card, then the material differences and dates are highlighted while the chosen order remains intact; an authorised editor can recheck fit and revise the order. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 7.6.2 | Given current facts cannot be refreshed, when the reader opens the card, then the last known dates and refresh limitation remain visible and unchanged is not asserted. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 7.7.1 | Given she holds the valid edit capability, when she revokes the card, then subsequent read and edit access is refused and successful revocation is confirmed. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 7.7.2 | Given the revocation request fails, when she returns to management, then the page states revocation has not been confirmed and offers retry rather than claiming access has ended. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 7.7.3 | Given she opens revocation information, when she reviews its effect, then it explains that previously downloaded or printed copies cannot be recalled and that losing the edit link prevents anonymous recovery. | **Deferred — later phase**. Not implemented in phases 0–3. |

## Epic 8

| AC | Exact authoritative criterion | State / evidence |
| --- | --- | --- |
| 8.1.1 | Given a baseline card and required times exist, when she selects a thirty-minute or one-hour extension to care end time, an unavailable first choice or unavailable institutional pickup, then the changed assumption and unchanged baseline appear together. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 8.1.2 | Given she enters her own valid one-off changes, when she runs a rehearsal, then the scenario uses those changes while the actual request and baseline card remain unchanged. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 8.1.3 | Given a baseline time is missing or the scenario times are inconsistent, when she tries to run it, then the required correction is identified and no result is produced from an invented time. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 8.2.1 | Given baseline and scenario results use the same evidence version, when she opens their comparison, then changed and unchanged condition states are distinguished for each backup with the assumption that caused each change. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 8.2.2 | Given a condition is unknown in either result, when she compares the backup, then uncertainty remains explicit and is not represented as a whole-arrangement pass or failure. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 8.2.3 | Given evidence changes during the comparison, when she opens the results, then she is prompted to rerun both sides using one evidence version rather than attributing a data change to the scenario. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 8.3.1 | Given multiple backups have the same relevant closing limit or delivery dependency, when she opens shared weaknesses, then the dependency, affected candidates and scenario impact are identified. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 8.3.2 | Given several backups lack transport or hours evidence, when she checks shared weaknesses, then the common information gap is distinguished from a known common service limitation. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 8.4.1 | Given the scenario exposes a conflict, when she chooses find alternatives, then the search page opens with the scenario request and triggering restriction clearly labelled, without silently replacing the saved baseline card. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 8.4.2 | Given the scenario exposes an unknown condition, when she chooses prepare questions, then the contact enquiry page opens for that candidate with the condition and scenario context as a question, not a confirmed failure. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 8.5.1 | Given a rehearsal has results, when she saves it to the card, then its assumptions, baseline revision, checked evidence versions, condition states and check time are retained. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 8.5.2 | Given a saved rehearsal exists, when an authorised reader opens it, then the assumptions, conflicts and unknowns are readable and clearly labelled as a dated rehearsal rather than a live confirmation. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 8.6.1 | Given relevant fact versions differ from a saved rehearsal, when she opens it, then a recheck prompt identifies the changed information without rewriting the saved result. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 8.6.2 | Given she reruns that scenario, when the comparison opens, then the same assumptions are retained and old and new condition states, evidence versions and check times appear with an explanation of differences. | **Deferred — later phase**. Not implemented in phases 0–3. |
| 8.6.3 | Given current information cannot be obtained, when she attempts a recheck, then the earlier snapshot remains dated and a failed-recheck message appears; its timestamp is not renewed. | **Deferred — later phase**. Not implemented in phases 0–3. |
