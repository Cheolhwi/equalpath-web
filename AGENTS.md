# EqualPath web

- The user authorised the second round on 2026-09-13: implement phases 4 and 5 against Revision 2.1 (10 stories / 23 AC), building on phases 0–3. Phases 6–8 remain outside this round.
- Preserve the flat RhineLab-inspired OSM search map, fine rules and quiet palette. The search map has no pitch, rotation or 3D buildings. No music.
- The user additionally requested a RhineLab-inspired landing page with Gaussian blur on 2026-09-12. The user subsequently simplified the entrance to one brief movement directly into the main interface: no zoom-out/pan sequence, intermediate screen or transfer labels. Preserve the same mounted flat map, reduced-motion controls, and in-memory request/comparison state when returning home. No automatic geolocation on the landing page. On 2026-09-13 the user explicitly requested the original RhineLabUI 3D scene with childcare elements for the landing page: reuse its licensed scene/models and interactions, add care props, and remove the map/crosshair/coordinates from the landing only.
- The latest landing revision is an extremely minimal full-screen art gallery: five user-supplied childcare illustrations, automatic back-and-forth cycling, manual previous/next and pause. On a fresh landing entry, show the full collection first, then automatically raise a readable childcare card after a brief pause. Return visits within the same session and reduced motion open directly on the raised card. Do not automatically focus the entry button on first load; preserve keyboard focus visibility. No right-hand marketing copy. Pause autoplay for interaction, hidden tabs, collection view and reduced motion.
- Existing prototype stays in `../design-prototypes/rhine-map`; new implementation lives here.
- Read public Appwrite `web_` resources only. The new `web-provider-query` function may be deployed for this authorized round; no old functions or owner rows may be changed. The user subsequently authorized the independent `Cheolhwi/equalpath-web` repository, CI/CD and publishing this website at the existing `equalpathcare.me` domain. Preserve other domain bindings and email DNS records.
- Real data and explicitly labelled controlled examples use separate modes. Never silently substitute fixtures for a failed live request.
- No parent account, child identity, automatic request history, messaging, booking, review browser, sharing or rehearsal in this round. Explicit favourites and public-place/time/transport templates are browser-local and separated by live/demo mode. Reuse requires a fresh service date and rechecks current facts. Preparation is a dated draft, not pickup authority or provider agreement; identity/health spaces are blank in exports and have no online input fields.
- Run local preview and verify the actual desktop/mobile user journey. Record data limits and unverified release gates honestly.

- On 2026-09-13 the user approved published opening hours as the default care-end window (specific care schedules/date exceptions take precedence), and sourced phone display instead of device-dialler acceptance checks. See docs/REQUIREMENTS_REVISION_2_1.md.
- The user subsequently requested public WhatsApp contacts and continued hours/transport collection. Display sourced WhatsApp links as an external user-initiated contact action; do not send messages or represent listing evidence as provider confirmation. Keep website enquiry numbers distinguishable from branch contacts. See docs/PUBLIC_CONTACTS_AND_SERVICES.md.
- The latest revision displays business closing as `Care end time` and requests admission-age fallback by institution type. Prefer sourced English/Malay branch age ranges. When unavailable, show the TASKA under-four or TADIKA 4–6 type reference with its official source, without asserting branch-specific acceptance. Keep specific care schedules, date exceptions and source discrepancies in the care-end display and sorting.
- The subsequent age-status revision displays a verified official type range as `Confirmed range`. Without a selected child age, use the separate `reference` state (no passed fit and no enquiry count); with a selected age, compare it to the official range. Preserve the official-type basis and separate temporary-admission check.
- On 2026-09-13 the user requested general OpenStreetMap place search with partial-name/typo matching, drag-to-confirm pickup, initial KL-centred nearby childcare, and restoration on return. Live `places` uses Photon independently of the childcare directory; never autocomplete against public Nominatim. The read-only `nearby` action lists current public centres without inventing a dated fit. Browser-local last map centre/zoom and selected pickup are now explicitly authorised; no automatic saving of child age, care dates/times or provider results. The revised location scope supersedes the earlier no-history rule only for this minimal last-map state.
- The user subsequently requested a first-visit tutorial with animated highlights, lightly dimmed surroundings, an immediate Skip, and prefilled examples that run through the workflow. The guided tour uses the real form, explicit demo-mode search API, condition view, comparison and enquiry components. Capture/restore the current request and selections on both Skip and completion; no example map, request, saved centre or contact may leak into real use. Persist only a versioned completed/skipped flag. Offer replay through Quick tour/settings, keep keyboard exit and reduced motion, and never auto-request device location or contact a provider.
- The subsequent discovery correction disables pan/zoom-triggered nearby queries: require Search this area at zoom 12+, meaningful movement, and reuse bounded browser cache. First/returning neighbourhood views and an explicitly selected pickup may load nearby centres. Select each page by proximity, then rank known conflicts below non-conflicts within that page. Show sourced fee bases and OSRM road-time estimates on searched results; batch/cache route requests, never derive drive time from straight-line distance or pretend to include live traffic. Highlight at most three mapped, conflict-free results with stronger relevant condition matches, with matching list badges; re-evaluate per result page/request.
- The latest search-radius revision caps both search and neighbourhood discovery at 10 km straight-line from the selected centre, defaulting to 10 km with an optional 5 km search. Enforce the cap in the API even for legacy/unlimited requests. Exclude missing/invalid coordinates before counting, pagination, suggestions and routing; never expand the radius automatically. Explicit saved-provider detail/compare lookups remain available by ID.
- The user subsequently explicitly authorised collecting published CariSchool fees and writing them to the new Appwrite database resources. Use versioned `fees_*` snapshots in `web_provider_evidence` and the existing public manifest; preserve base catalogue, hours/services snapshots and all owner resources. Capture fee bases, programmes, extras and attribution; never import a submission-field placeholder (Holy Light's 450 is one). List sourced KPM codes clearly, without treating a directory code as independent official-register verification. See docs/DIRECTORY_FEES_2026_09_13.md.
- The user then requested broader named-provider fee/hour collection and at least one thousand fee entries, allowing comparable-area estimates for gaps. Preserve actual prices first; budget references use at least five distinct same-type priced providers, district then region/service-area fallback, and a visible `Estimated` label. Do not feed estimated, subsidy, registration-only or open-ended prices back into the reference pool. Official clock ranges without weekdays are visible evidence without inventing service dates. This scope authorises publication to the new web snapshots, not owner resources.
- The user subsequently requested price-aware sorting and registration icons on result cards. Offer Lowest monthly fee in search and comparison; use the lowest MYR monthly programme amount/range start, include labelled area budgets, exclude extras and incomparable billing periods, and keep known conflicts last. Map suggestions follow price priority. Registration icons reveal the number and source: distinguish imported JKM records from directory-listed KPM codes, and show expired/future/unresolved records as needing attention. Do not invent government approval or availability.
- The latest clarification selects each 20-item page by nearest straight-line distance inside the radius, before sorting that page by conflicts and the selected priority. Nearby conflicting centres must not be displaced into later pages by farther non-conflicts. Keep stable distance/ID page membership across priorities; explicit user filters still apply. Conflicting results remain on the page after non-conflicts; their map pins are muted but selectable. JKM and KPM use the same small sage check-seal icon; their actual authority, number and evidence status appear on click. Expired/future/unresolved records retain the attention state.

- The centre-details layout follows the user’s decision order: branch identity and current visit, four key facts (care end, age, driving and fee), condition checks with conflicts first, and a single grouped contact/compare/save/preparation area. Conflicts open by default; retain expandable explanations, fee bases, weekly schedules and registration/source provenance. Preserve tour targeting and the existing no-contact side-effect boundary.
- The map overlay has no service-region or marketing heading: KL + Selangor describes coverage, not the current address. Use a compact count/pickup/suggestion legend and the pickup-selection action. The user subsequently removed Search this area as a redundant entry point. Initial/returning views and confirmed pickup changes may load nearby centres; ordinary map pan/zoom must not query. Keep the map clear, with responsive wrapping.
- The user removed straight-line distance displays across the UI. Comparison Location contains the address only; nearby cards and map previews do not show geographic-distance numbers. Use Nearest first and Search radius labels; retain road distances/driving times and internal geographic filtering/pagination. Do not relabel geographic distance as road distance.

- The preparation page now uses “Get ready for care”: show the chosen centre/date, a three-stop pickup plan and grouped contact/export tools before supporting detail. Show care-centre questions first; fold pickup-place and driver questions, retain a tickable packing list with progress and separate sourced requirements. Pickup may be any public place, not necessarily a usual centre. Print/download use the same plain-language structure. Keep the dated draft, explicit update after a changed request, unresolved arrival time, privacy blanks and no automatic contact.
- Search and comparison sorting use the shared themed SelectMenu rather than the OS select popup. Preserve selected checks, disabled priorities, arrow/Home/End/type-ahead navigation, Enter selection, Escape/Tab/outside dismissal, viewport-aware placement and light/dark styles. Merely opening or navigating the menu must not issue a search. Sorting, nearest-page membership and map recommendation rules are unchanged.
- The landing entry uses a brief ivory curtain sweeping from right to left, inspired by the user's ignoredone.space sidebar-navigation reference. Cover the landing, then reveal the already-mounted search interface in 1200 ms total (480 ms cover / 720 ms reveal). The curtain contains the current childcare artwork and EQUALPATH wordmark in a responsive paper-frame composition; freeze the artwork on entry and pause scene autoplay. Mount and decode all five transition images while the landing is open, with the same anonymous request mode as the scene textures; reuse those image elements during entry. If the selected artwork is unavailable, enter directly instead of showing an empty frame or waiting on the network. Do not add an intermediate destination. Reduced motion enters immediately; Escape finishes the transition, and the existing request/map/comparison state remains intact.
- The same reference informs gentle upward reveals for result cards, dialogs and small popovers, plus a sage circular desktop pointer with hover/press feedback. Cards reveal once when first visible in 480 ms, with a stagger capped at 210 ms; dialogs use 440 ms and small popovers 240 ms. Animation must not trigger data requests or delay access. Preserve native text-entry and map-dragging cursors, keyboard dismissal/focus, touch interaction, reduced motion and high-contrast fallback. The decorative pointer never receives input or hides the native cursor before it is visible.

- Transition artwork sits on white paper without multiply blending. The display filter neutralises near-white ivory pixels while keeping the source files, scene textures and surrounding page palette unchanged. Preserve the preloaded image elements and existing motion timing.

- Search and saved-template time fields use the shared themed TimeInput, with sage selections instead of the OS blue popup. Preserve minute-precise HH:mm values, direct typing, same-day validation, explicit Done/Clear, keyboard navigation, cancellation, viewport placement and modal/light/dark/reduced-motion behavior. Opening or adjusting the picker must not search or save automatically.
- Comparison uses plain parent-facing copy: Compare childcare, Your shortlist, and Find childcare for the return action. Distinguish no selections from one selection, explain how to add options, and retain condition/source/fee meanings and all comparison selections.
- Saved uses “Saved for later”, Childcare/Searches tabs and “Use this search”. Lead with saved names, pickup place and separate time fields; fold source dates and place storage guidance below the items. If the current mode has saved searches, show a sage reminder above the Find childcare form: one search can be loaded directly, multiple searches open the Searches tab. Reuse is explicit and always clears date/age for the new request. Do not restore a search automatically; keep live/demo isolation, tour suppression, deletion updates and browser-local storage.
- The questions page leads with the chosen centre and dated visit, then plain-language questions alongside copy/contact tools. Every condition-derived question shows the matching check name, status and short reason; expand it to read the original assessment and its source. Availability and final fees are labelled “Ask for every visit”, separately from unresolved checks. The API retains question membership and stable IDs; the view puts conflicts first by default and preserves the user's selected order for copying. Matching details are not reintroduced. Keep preview/manual-copy fallback, optional pickup/age explanations, sourced external contacts and a secondary “After you’ve spoken” checklist action. No automatic contact, booking or provider-status mutation.
- On 2026-09-14 the user requested contact-first recommendations. Preserve nearest-20 page membership within the 10 km cap and known-conflicts-last ordering; within each conflict group, prioritise a displayed phone or explicit WhatsApp contact before the chosen sort. A website alone is not a contact for this rule. Search/map suggestions and comparison winners first restrict their conflict-free pool to contactable centres. Do not fill spare recommendation slots with no-contact centres; use them only when that eligible pool has no contactable centres. Preserve the selected priority’s data requirements, including comparable monthly fees, and never promote conflicts or invent contact details.
- The user clarified that contact priority is an internal recommendation rule: do not explain it in the search, comparison or recommendation UI. Keep the existing recommendation highlights and normal contact actions; no contact-priority badge or explanatory copy.
- The landing starts with a light branded loading surface containing EQUALPATH and “Find childcare that fits your day.”, plus a small explicit “Loading…” status and subtle wordmark opacity pulse. No slash, paper-card icons, progress line or “artwork” copy in this loader. Its visibility follows actual scene readiness: remove it immediately after the loaded scene draws its first frame, with no post-load fade or forced minimum wait. Report the scene and parent ready states together; do not defer loader dismissal through a passive effect. Keep Find childcare available during loading/errors, with immediate entry, a retry for failed loading, a slow-load hint and static reduced-motion presentation. Direct #discover entry bypasses it. Preserve the existing collection-to-raised opening, landing-to-search transition and map/request state. The main header wordmark uses the landing's MiSans family, 750 weight, tight tracking and light slash; keep other app typography unchanged.
- Reuse the ready gallery and decoded transition images when returning from search: retain them hidden, pause rendering/autoplay while inactive, and never reset successful readiness to loading. Restore the existing raised READ view without rebuilding or requesting the images again. Incomplete scenes may be discarded when the user enters search early. Do not use a saved boolean to pretend resources remain loaded across a fresh document; actual resource/scene readiness remains authoritative.
- Main dialogs (details, comparison, saved items/editors, preparation, questions and information/settings) retain their last content for a 360 ms fade/downward exit with a matching backdrop fade. Close controls, Escape, backdrop and content return actions share this lifecycle. Keep the native modal open and its content inert during exit, restore trigger focus without scrolling after removal, ignore repeated dismissal and provide a cancelled-animation timeout. A quick close starts at the entrance's current opacity/transform. Reduced motion and tutorial handoffs dismiss immediately. Preserve opening timings, queries, storage and contact behavior.
- On mobile, the selected-centre map preview shows only the name (up to two lines), driving time and fee. Keep the fee basis and estimate label, use a compact tappable card to reach the existing details/request flow, and leave room above map attribution and any comparison tray. Address, condition status and the separate action row remain in the desktop preview and full details.
- Search and comparison offer only distance, monthly fee, care-end time and pickup-service priorities. The user removed the alphabetical `By name` menu option; do not reintroduce it.
- On 2026-09-15 the user explicitly requested collecting and publishing evidence for bringing a child for a few hours. The independent `admissions_*` snapshot in public `web_provider_evidence` records reviewed branch identity, service scope, source/capture timestamps and contextual questions. Published branch hourly/drop-in descriptions may support the temporary-admission service check; brand-only programmes, preschool extensions and paused-new-enrolment notices must not become confirmed branch acceptance. Keep same-day capacity, notice, age and fees separate, preserve owner resources and other snapshots, and do not contact providers. See docs/TEMPORARY_CARE_2026_09_15.md.
- The user then explicitly authorised adding new physical short-care providers missing from the original catalogue. The independent `additions_*` snapshot appends reviewed, contactable, located branches after the existing overlays. Unknown registration uses `CHILDCARE` with no KPM/JKM badge or official type-age fallback. Preserve published hourly fee periods, branch-specific care schedules, conditional weekend/notice rules and source discrepancies. Do not count unopened centres or ambiguous address matches as current usable candidates. See docs/SHORT_CARE_EXPANSION_2026_09_15.md.
- The next expansion retains explicit service requirements (advance notice, maximum stay, toilet training or separate supervised-service booking) in the temporary-admission question even when the service itself is published. Advance-booked daily childcare and parent-can-leave play/art sessions are labelled by their actual scope; do not present them as unrestricted same-day registered childcare. Preserve day/hour fee bases and programme hours; ordinary playground tickets and venue opening hours do not establish drop-off prices or staffed-care hours. See docs/SHORT_CARE_DEEPER_2026_09_15.md.
- The user clarified that the roughly 100-location short-care research target remains limited to Kuala Lumpur and Selangor. The third pass verified 21 published-service locations in total, not 100. Preserve old profile dates, address discrepancies and holiday-only restrictions in the three new service records. A scan of 657 directory profiles is research coverage, not a count of providers offering short care. See docs/SHORT_CARE_RESEARCH_2026_09_15.md and the sourced location list in docs/SHORT_CARE_LOCATIONS_2026_09_15.md.
- On 2026-09-15 the user requested separate regular-childcare and short-term-care searches. Default to regular care with location and optional age/pickup preference; no service date or care-time requirements. Short-term care requires a date, collection deadline and care end, and reads only the 101-provider `profile_evidence_*` collection already imported into the public database. Regular care excludes those IDs. Filter by membership before the 10 km radius, nearest-20 pagination and ranking; nearby maps, details and comparisons use the same pool. Switching clears prior selections and stale responses, while templates explicitly retain the care type. Legacy dated templates remain short-term. The research collection is labelled as a coursework demo once in the search UI; membership must not overwrite admission evidence, same-day availability or provider registration. Regular enquiries and checklists use enrolment/recurring-care language without inventing a date or hourly fee. See docs/CARE_TYPES_2026_09_15.md.
- The user's subsequent radius revision limits short-term care to 5 km and 10 results per page; short-care nearby discovery also returns at most 10. Apply this in the API to explicit, legacy and oversized requests before counts, pagination, suggestions and routing. Regular care retains its default 10 km maximum (optional 5 km) and 20 results per page. Keep the existing separate membership pools, nearest-page selection, contact preference and conflict-last ranking. UI radius options, pagination and ordering text must use the active care type's limits.
- Short-term search and comparison use the label “Lowest fee”. Only short-stay hourly, visit, session or daily care rates (or an existing validated visit total) appear in short-care results, nearby/map previews, details, comparisons and enquiry summaries. Monthly/term/year budgets and extras are not short-stay prices; do not convert them into made-up hourly rates. Missing short-stay prices show “Ask the centre”. Price sorting groups complete estimated totals, hourly, visit, session and daily rates in that order, comparing the lowest starting amount within each period. Comparison winners must share the same billing period. Regular care retains its monthly-fee sorting and display.
- Unavailable sorting must not stay active after changing location, filters, date, page or comparison selection. The API checks the actual page/selection, falls back to `distance` before sorting and suggesting, and returns the effective request sort; comparison state must adopt that returned value. Changing priority on an unchanged search preserves its current page. Disabled choices explain the missing data concisely: “No fees listed nearby.”, “No fees on this page.” or “No fees listed.” in comparison, with monthly wording for regular care and specific reasons for hours/pickup/currency. Do not use a bare “Unavailable” or a full sentence repeating the radius.
